# Batch 11 intake examples

These files use fictitious example data. They illustrate request bodies for lead intake, conflict screening, and matter opening.

Matter opening is intentionally blocked until the linked client onboarding record contains a conflict check that a lawyer has marked `cleared` or `overridden`.
