# Browser requests stay same-origin through the Next.js rewrite so secure session cookies
# and CSRF protection work without exposing the FastAPI origin to client code.
NEXT_PUBLIC_API_URL=/backend/api/v1
API_INTERNAL_URL=http://localhost:8000
