"use client";
export default function GlobalError({error,reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="page"><div className="eyebrow">Something went wrong</div><h1 className="page-title">This workspace could not load</h1><p className="page-subtitle">Your data has not been changed. Retry the request; if it keeps failing, use the request or error reference when contacting your administrator.</p><section className="notice-panel" role="alert">{error.message||'Unexpected application error'}{error.digest?` · ${error.digest}`:''}</section><button className="primary-button" onClick={reset}>Try again</button></div>}
