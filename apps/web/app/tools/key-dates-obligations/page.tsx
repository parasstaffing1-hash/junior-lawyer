import { KeyDatesTool } from "@/components/tools/key-dates-tool";

export const metadata = {
  title: "Key dates & obligations — free legal tool",
  description: "Extract dated obligations and deadlines from contract text.",
  alternates: { canonical: "/tools/key-dates-obligations" },
};

export default function Page(){return <KeyDatesTool/>;}
